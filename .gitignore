# Rust build artifacts
/target/
*.tar.gz

# Frontend build output
web/dist/
web/node_modules/

# Credential-bearing config files — keep *.example variants instead
config/api.toml
config/poller.toml
config/neteco.toml
config/staging/api.toml
config/staging/poller.toml
config/staging/neteco.toml

# Environment files with secrets
.env

# Editor / OS
.DS_Store
*.swp
.idea/
